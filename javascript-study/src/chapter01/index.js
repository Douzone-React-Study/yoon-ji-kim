let value = 1; // 변수
console.log("value:" + value);
value = 2;
console.log("value:" + value);
const a = 1; //상수
console.log("a:" + a);
// a = 2;  상수로 선언했으면 값을 바꿀 수 없음
// 모던 JS에서는 var 사용 XX

// 데이터 타입
// 숫자, 문자열, Boolean,
// 없음 : null( 값이 없다), undefined(값이 설정되지 않았다)
const friend = null;
let criminal;
console.log(criminal);
