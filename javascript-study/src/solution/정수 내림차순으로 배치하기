function solution(n) {
    //n을 문자열 배열로 변환
    var arr = n.toString().split('');
    //정렬
    arr.sort().reverse();
    //join(구분자): 배열 요소 연결해서 문자열로 만듦
    //문자열 만들어서 숫자로 변환
    var answer = Number(arr.join(''));
    return answer;
}